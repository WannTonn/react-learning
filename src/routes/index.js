
import CommonConfig from './config.common';
import Layout from "@/views/Layout";
/* export default [
  {
    path: '/',
    element: Layout,
    routes: [
      ...CommonConfig,
      {redirect: '/todolist'},
    ]
  }
] */
export default [
  ...CommonConfig
]